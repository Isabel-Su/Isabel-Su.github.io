# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Full-resolution originals. These are the input to `npm run images`; only the
# compressed output in src/assets/images/ is committed, which keeps the repo
# small. Keep your originals backed up somewhere else.
assets-raw/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
